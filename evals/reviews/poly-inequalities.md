Lesson: poly-inequalities
Harness: PASS  Screenshots: yes
1 Accuracy: 4/4 - $p(x)=(x+2)(x-1)(x-3)$ has critical points $-2,1,3$, the sign pattern $-,+,-,+$, solution $(-2,1)\cup(3,\infty)$, and $p(2)=-4$, all matching the sign table and shaded figure, and the quiz (squared factors $\{1\}\cup[4,\infty)$, $x^3\le4x\Rightarrow(-\infty,-2]\cup[0,2]$) is correct throughout.
2 Prereqs: 4/4 - Assumes factoring and the zero-product idea, names the three-step method up front, and introduces one idea per slide (boundaries, then test each interval, then write the solution).
3 Pedagogy: 4/4 - The method is stated as three steps, the sign chart is walked region by region with each factor's sign shown above the parenthesized term, and both drag questions start off-answer (slide 1 rests at $x=2$ where $p<0$ and asks for $p>0$; slide 2 ends at $x=4$ where $p>0$ and asks for $p<0$).
4 Wording: 4/4 - Concise and precise with direct "make sure to test strictly inside each interval" callouts, terms bolded once, no em dashes or semicolons after cleanup.
5 Visual: 4/4 - A full sign chart (row per interval, column per factor plus the product), region signs $-,+,-,+$ above the axis, factor-sign annotations on the expression, and correct open circles at the strict-inequality endpoints.
6 Interaction: 4/4 - The slider drives the tracer with a live signed $p(x)$ readout, is reversible, and neither manipulate question rests on its answer.
7 A11y: 4/4 - Native range slider (aria-valuetext), keyboard-navigable choices, figure aria and live readout for audio-off, reduced motion via the engine, and no plot questions, so every task is keyboard-operable.
Blocking issues:
- None.
Top 3 fixes:
1. When $|p|$ is large the tracer dot clamps to the top edge (e.g. $p(4)=18$); add a small "off the top" indicator so the clamp is not misread as a value.
2. Label the shaded solution set on the axis (e.g. a small $p>0$ tag) so the shading ties to the inequality being solved.
3. Add a non-color cue (hatching or a pattern) to the shaded region so the solution does not rely on green alone.
Verdict: SHIP
