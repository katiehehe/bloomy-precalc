Lesson: rational-inequalities
Harness: PASS  Screenshots: yes
1 Accuracy: 4/4 - $r(x)=\frac{x-3}{x+1}$ has a numerator zero at $x=3$ (solid) and a wall at $x=-1$ (open), sign pattern $+,-,+$, solution $r>0$ is $(-\infty,-1)\cup(3,\infty)$ and $r\ge0$ is $(-\infty,-1)\cup[3,\infty)$, and the quiz (combine-first, $(2,3]$, $(-\infty,-1)\cup(1,3]$, walls always open) is correct.
2 Prereqs: 4/4 - Reuses the polynomial three-step method as an explicit prerequisite, then adds the single new idea that a fraction has two kinds of critical point, one idea per slide.
3 Pedagogy: 4/4 - The roadmap is reused, the sign chart is tested region by region, the solid-versus-open distinction is justified, and both drag questions start off-answer (slide 1 rests at $x=0$ where $r<0$ and asks for $r>0$; slide 2 ends at $x=4$ where $r>0$ and asks for $r<0$).
4 Wording: 4/4 - Concise, with "make sure to always exclude a wall" callouts, terms bolded once, math in $...$, no em dashes or semicolons after cleanup.
5 Visual: 4/4 - A solid dot marks the numerator zero and an open dot the wall, the sign chart shows both factors plus the quotient, the solution shades with open endpoints, and the fraction readout annotates each factor's sign.
6 Interaction: 4/4 - The slider drives the tracer with a live signed $r(x)$ readout, is reversible, and neither manipulate question rests on its answer.
7 A11y: 4/4 - Native range slider (aria-valuetext), keyboard-navigable choices, figure aria and live readout for audio-off, reduced motion via the engine, and no plot questions, so every task is keyboard-operable.
Blocking issues:
- None.
Top 3 fixes:
1. Draw (or annotate) the horizontal asymptote $y=1$ so the two branch shapes are unambiguous, since the curve visibly levels there.
2. Label the shaded solution set on the axis so the shading ties to the inequality being solved.
3. Add a non-color cue to the shaded region so the solution does not rely on green alone.
Verdict: SHIP
